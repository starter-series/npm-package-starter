name: CodeQL
'on':
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
  workflow_dispatch: {}
permissions:
  contents: read
  security-events: write
jobs:
  codeql:
    uses: starter-series/.github/.github/workflows/reusable-codeql.yml@fdacdbce9d1dd18a573e7c57c482bb07ab1f2954
    with:
      language: javascript-typescript
  analyze:
    needs: codeql
    if: ${{ always() }}
    runs-on: ubuntu-latest
    steps:
    - uses: starter-series/.github/.github/actions/require-checks@fdacdbce9d1dd18a573e7c57c482bb07ab1f2954
      with:
        results: ${{ toJSON(needs) }}
